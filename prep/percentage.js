function calculatePercentage(decimalNumber) {
  let percentage = decimalNumber * 100;
  
const decimalNumber = 0.25; // declaration
let percentage = decimalNumber * 100; // statement
return percentage;

console.log(calculatePercentage(0.25) + "%"); // Output: 25%
}